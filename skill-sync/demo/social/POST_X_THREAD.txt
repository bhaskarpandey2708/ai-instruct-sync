1/3
Hot take: most teams don’t have an agent-skill system.
They have a graveyard of markdown files named final, final2, use_this_one.

2/3
I shipped skill-sync — free local OSS alpha.

• validate skill packages (name / version / skills[])
• plan install: added vs updated
• --json for CI
• zero runtime deps

npx @bhaskarauthor/skill-sync

3/3
Not a full registry yet. Just the discipline layer so Claude, Cursor, and friends stop running three different “sources of truth.”

Video ↓
